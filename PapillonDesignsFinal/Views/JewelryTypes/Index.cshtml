@model IEnumerable<PapillonDesignsFinal.Models.JewelryType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JewelryTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JewelryTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JewelryTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JewelryTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JewelryTypeId })
        </td>
    </tr>
}

</table>
